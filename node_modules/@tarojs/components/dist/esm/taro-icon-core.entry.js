import { r as registerInstance, h } from './core-42efd82a.js';
import { c as classNames } from './index-be61bb74.js';

const Icon = class {
    constructor(hostRef) {
        registerInstance(this, hostRef);
        this.size = '23';
    }
    render() {
        var _a;
        let { type, size, color } = this;
        const iconType = (_a = type) === null || _a === void 0 ? void 0 : _a.replace(/_/g, '-');
        const cls = classNames({
            [`weui-icon-${iconType}`]: true
        });
        const style = {
            'font-size': `${size}px`,
            color
        };
        return (h("i", { class: cls, style: style }));
    }
};

export { Icon as taro_icon_core };
