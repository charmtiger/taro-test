'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const core = require('./core-288f35a6.js');
const index = require('./index-00392f71.js');

const Icon = class {
    constructor(hostRef) {
        core.registerInstance(this, hostRef);
        this.size = '23';
    }
    render() {
        var _a;
        let { type, size, color } = this;
        const iconType = (_a = type) === null || _a === void 0 ? void 0 : _a.replace(/_/g, '-');
        const cls = index.classNames({
            [`weui-icon-${iconType}`]: true
        });
        const style = {
            'font-size': `${size}px`,
            color
        };
        return (core.h("i", { class: cls, style: style }));
    }
};

exports.taro_icon_core = Icon;
